# RETIRED — kept for reference only, no longer used by cicd/Dockerfile.backend.
# Substrait gateway is fronted by nginx-ingress; the desired limits were:
#   client_max_body_size 40m
#   proxy_read_timeout 300s
#   proxy_connect_timeout 75s
#   proxy_send_timeout 300s
#   keepalive_timeout 300s
# A local in-pod nginx was removed because the platform drops all Linux
# capabilities and stock nginx dies at startup on chown() (pods stay NOT READY,
# ingress returns 502). uvicorn now serves port 8000 directly with
# --timeout-keep-alive 300; upload limits are enforced in app code + frontend.
